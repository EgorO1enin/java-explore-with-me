spring:
  application:
    name: ewm-main-service
  datasource:
    url: jdbc:postgresql://stats-db:5432/ewm_stats
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  sql:
    init:
      mode: always
      schema-locations: classpath:init-db/02-init-main-db.sql

server:
  port: 8080

logging:
  level:
    ru.practicum.ewm: INFO
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.springframework.data.jpa: DEBUG
    org.springframework.transaction: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

# Конфигурация для stats-client
stats:
  client:
    url: http://stats-server:9090
    connect-timeout: 5000
    read-timeout: 10000
